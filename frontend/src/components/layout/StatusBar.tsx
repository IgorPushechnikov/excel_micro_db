// frontend/src/components/layout/StatusBar.tsx

import React from 'react';

const StatusBar: React.FC = () => {
  // StatusBar уже отображается в footer MainLayout.tsx
  // Этот компонент можно использовать для более сложной логики статусной строки в будущем
  return null; // Пока возвращает null, так как содержимое отображается в MainLayout
};

export default StatusBar;